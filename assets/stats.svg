<svg xmlns="http://www.w3.org/2000/svg" width="980" height="200" viewBox="0 0 980 200" role="img" aria-labelledby="title">
<title id="title">Statistics awaiting the first GitHub Actions run. No activity numbers are shown yet.</title>
<defs></defs><style>
text { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif; }
.mono { font-family: "SFMono-Regular", Consolas, "Liberation Mono", monospace; }
.orbit { transform-box: fill-box; transform-origin: center; animation: orbit 24s linear infinite; }
.reverse { animation-direction: reverse; animation-duration: 34s; }
.glow { animation: glow 5s ease-in-out infinite; }
.travel { stroke-dasharray: 16 84; animation: travel 8s linear infinite; }
.float { animation: float 6s ease-in-out infinite; }
.typing { animation: typing 10s steps(40, end) infinite; }
.cursor { animation: cursor 1.3s steps(1) infinite; }
@keyframes orbit { to { transform: rotate(360deg); } }
@keyframes glow { 0%,100% { opacity: .4; } 50% { opacity: .95; } }
@keyframes travel { to { stroke-dashoffset: -100; } }
@keyframes float { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-7px); } }
@keyframes typing { 0%,8% { width: 0; } 45%,88% { width: 480px; } 100% { width: 0; } }
@keyframes cursor { 50% { opacity: 0; } }
@media (prefers-reduced-motion: reduce) {
 .orbit,.reverse,.glow,.travel,.float,.typing,.cursor { animation: none !important; }
 .typing { width: 480px; }
 .travel { stroke-dasharray: none; }
}
</style>
<rect width="980" height="200" rx="20" fill="#0b1020"/>
<text x="30" y="49" fill="#aab8cd" font-size="14" letter-spacing="1.5">GITHUB / PUBLIC ACTIVITY</text>
<text x="30" y="98" fill="#f0f5fc" font-size="27" font-weight="600">Your real stats belong here.</text>
<text x="30" y="137" fill="#aab8cd" font-size="17">Run the Update profile workflow to connect your GitHub account.</text>
<path d="M30 173 H950" stroke="#69f0c2" stroke-width="2" pathLength="100" class="travel"/>
</svg>
